import { List, ListItem, Stack, Typography } from '@mui/material';
import * as React from 'react'

function ArticlesList() {
    return <List component={Stack} direction="row">
        
    </List>;
}

export default ArticlesList;